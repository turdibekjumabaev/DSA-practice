#include <iostream>
#include <math.h>

using namespace std;

int main(){
    int a, b, c, d;
    float x1, x2;

    cin >> a >> b >> c;

    x1 = (-b+sqrt(d)) / (2*a);
    x2 = (-b-sqrt(d)) / (2*a);

    cout << x1 << endl << x2 << endl;
}