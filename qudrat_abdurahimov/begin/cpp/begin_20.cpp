#include <iostream>
#include <math.h>
using namespace std;

int main() {
    int x1, y1, x2, y2;
    cin >> x1 >> y1 >> x2 >> y2;
    float x = sqrt(pow(x1-x2) + pow(y1 - y2))

    cout << x << endl;

    return 0;
}