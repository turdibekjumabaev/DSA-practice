#include <iostream>
#include <math.h>

using namespace std;

int main(){
    int a;
    cin >> a;

    cout << pow(a,2) << endl << pow(a, 3) << endl << pow(a, 5) << endl << pow(a, 10) << endl << pow(a, 15);
}