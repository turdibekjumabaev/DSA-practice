#include <iostream>
#include <math.h>
using namespace std;

int main(){
    int a, b, ab;
    cin >> a >> b;
    ab = a * b;
    cout << ab << endl << abs(a) << endl << abs(b) << endl;

    return 0;
}